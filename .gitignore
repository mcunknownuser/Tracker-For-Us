node_modules
dist
.env.local
.env*.local
.DS_Store
*.log
.vite
.vercel

# Tauri — build artifacts only; commit src-tauri/ source itself.
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# Supabase CLI local state — written by `supabase link`, machine-specific.
# Migrations and functions ARE committed; only the CLI's scratch dir is not.
supabase/.temp
